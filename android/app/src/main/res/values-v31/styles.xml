<resources>
    <!-- Android 12+ splash screen -->
    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo_android12</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>

        <!-- Remove the icon background "circle" behind the splash icon (if present). -->
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <!-- Use a padded logo so it won't be clipped by the system mask. -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/bootsplash_logo_android12</item>
        <item name="android:windowSplashScreenBackground">@color/bootsplash_background</item>
    </style>
</resources>

